## Installation

- Clone the repository
- `cd` inside the folder
- run commands:
- `npm i`
- `cd client/`
- `npm i`
- `cd ..`
- `npm run dev`

## Deploy in heroku

- Create app in heroku, then run
- `git push heroku master`
